# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Fal AI (blog thumbnail generation in admin — optional)
FAL_KEY=

# n8n Webhook URL for the Contact form (server-side only — set in Vercel env)
N8N_CONTACT_WEBHOOK_URL=
